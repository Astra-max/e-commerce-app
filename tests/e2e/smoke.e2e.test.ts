import { describe, it, expect } from 'vitest';

// Simple smoke e2e placeholder. This ensures the test runner and environment are wired up.
describe('e2e smoke', () => {
  it('basic environment smoke check', () => {
    expect(1 + 1).toBe(2);
  });
});